# 交付验证记录

此文记录各次验证的实际范围，不代表所有系统或游戏均已验证。使用与构建步骤见 [README](../README.md)。

## 2026-09-25 双 target 与用户态 XY / 鼠标五键

- 原 target 改名为 `mouse_input_mapping_kernel`，保留 X 过滤、自动驱动部署和 `config.ini`；新增 `mouse_input_mapping_user`，独立使用 `config.user.ini`，以 Raw Input + SendInput 映射 XY 和 LMB/RMB/CMB/X1/X2。
- 被外部恢复的构建、配置、测试及文档修改已恢复，Debug / Release 重新编译成功，两组各 9 项 CTest 通过；状态机/配置测试共 1,148 个断言。原有 `runtime_integration` 此前尝试执行时，被当前运行中的旧版程序占用公共单实例锁阻挡，复测排除此项；没有结束用户的程序，也不将这项记为通过。
- 新增测试覆盖双轴斜向、独立超时、Y 反向、五键配置及冲突、按钮按住/松开、同包点击、组合、多鼠标占用、拔出释放、实体键合并、失败清理重试。
- 用户态回调测试使用模拟 Raw Input 和内存 SendInput 接收端，覆盖真实回调中的开关防连发、注入事件回环排除、扩展扫描码、绝对设备按钮、设备移除及读取/发送失败；没有生成真实系统输入。
- 用户态真实鼠标操作、目标游戏响应及反作弊兼容性尚未验证。游戏内需关闭鼠标输入并绑定对应键盘动作；测试通过不代表游戏会接受 SendInput。

## 2026-09-25 项目结构整理

- 使用 CMake 3.29.2、MSVC 19.51、Ninja，在全新 `build/release/` 和 `build/debug/` 目录构建成功；两组 CTest 均为 **7/7 通过**。
- 首轮 Release 集成测试遇到单实例互斥锁占用，后续重试通过；运行测试时应关闭本工具并避免同时运行另一组集成测试。
- 安装到 `dist/windows-x64/`，通过 CPack 生成版本化 ZIP；检查包含 MIT 许可证、第三方声明、原始许可和用户态库源码，未混入个人配置或测试程序。
- 此次未安装驱动、修改输入逻辑或重新执行真实鼠标/游戏验收。GitHub Actions 工作流已添加，远端运行结果需推送后确认。

## 已验证

- 在 Windows x64、系统构建号 26200 上，使用 MSVC 19.51、Windows SDK、CMake/Ninja 构建 Release 和 CLion Debug 成功，编译无警告。两个构建的 CTest 均通过。
- CTest **7/7 通过**：状态机/配置测试、直接控制台按键录入、内置资源释放、主程序帮助、检测工具帮助、驱动接口模拟集成、终端配置与错误路径。
- 状态机/配置测试 **1,102 个断言通过**，覆盖抖动、慢移、快速反向、超时、8 kHz 输入序列、实体按键合并、失败清理、扫描码与配置落盘，并验证 F1–F24 和扩展键的名称回显。
- 独立隐藏控制台测试通过 `WriteConsoleInput` / `ReadConsoleInput` 验证 F12、LEFT、F8 捕获和 Enter 确认；Enter 自动重复不跳过下一项，Pause 不绑定，退出后恢复控制台模式。未向真实键盘输入路径注入任何事件。
- 文本/IDE 模式验证名称回显、二次回车确认；输入结束而未确认时不覆盖原配置。
- 将 EXE 单独复制到无 DLL 的目录后，`--check` 成功加载内置 DLL，并到达驱动状态检查。内置 DLL 与安装器均经逐字节释放验证；退出后临时文件和目录删除。
- 模拟驱动集成验证 **19 个输入步骤、16 个输出包**：关闭时透传、长按快捷键只切换一次、清零 X、保留 Y/按钮/滚轮/标记、先释放再反向、停止释放、实体键持有、绝对输入透传、读取失败后释放按键。
- 2026-09-25 过滤器启动修复：本机直接查询 `IOCTL_GET_FILTER` 返回成功但输出长度为 0，官方 DLL 因此返回 0。模拟 DLL 现复现该行为，并检查过滤器仍被设置、退出时被清零；旧版 EXE 在此场景复现 `Cannot enable driver input filters`，修复版完整通过输入包集成测试。Release 和 Debug 的 CTest 均为 **7/7 通过**。
- 本机真实驱动 `--check` 成功枚举 7 个键盘和 4 个鼠标。修复版使用 A/D/F8 配置成功进入 OFF 事件循环，再由独立隐藏控制台的 Ctrl+C 正常退出，打印按键清理信息，退出码 0。此项只验证启动和退出，不构成开启映射后的真实输入效果验证。
- 独立 Raw Input 检测工具可启动并按 `--seconds 1` 正常退出；此次无真实鼠标输入，**不构成屏蔽效果验证**。
- 主程序使用 Windows SDK 系统库和静态 MSVC 运行库；动态加载官方 `interception.dll`，后者依赖系统自带的 `msvcrt.dll` 与 `KERNEL32.dll`。
- 交付目录的 Interception DLL 与固定版本的官方 x64 DLL 的 SHA-256 一致：`ab88164c11b1b48488772d4c3bfaa4509d5b0ae9dbc5a691dc4f96f0260443c8`。
- 附带的上游用户态库源码通过新增的独立 CMake 构建入口编译成功；交付仍使用官方发布 DLL。

## 尚未验证

早期验证时本机驱动不可访问；2026-09-25 复测时驱动已可访问，真实启动与退出已通过。此次修复未执行系统驱动安装、重启或修改系统安全设置。

首次管理员安装到重启后的完整流程尚未在自动测试中验证。实际 Raw Input 横向屏蔽、Y/按钮/滚轮透传、真实高回报率鼠标延迟和具体游戏效果仍需按 `README.md` 验证。用户尚未指定目标游戏。模拟接口测试不能替代这些实测。
## Steam daemon 模式（2026-09-25）

使用 MSVC 19.50 在独立目录完成 Debug / Release 构建；两组各 11 项 CTest 全部通过。新增测试覆盖两个版本对直接启动进程的退出监听、带空格和引号的参数转发、关闭进程句柄后子进程仍运行，以及用户态缺失键位时拒绝启动。

真实 Steam 游戏尚未验证。先为两个版本分别保存完整键位配置，并完成内核驱动准备。使用 README 中的 Steam 启动选项验证游戏收到参数和 Steam 工作目录。工具日志应显示游戏 PID；结束游戏后应显示游戏退出与映射按键释放。再次启动游戏，单独关闭工具控制台，确认游戏保持运行。若命令指向启动器，只以该直接进程的退出为准。
